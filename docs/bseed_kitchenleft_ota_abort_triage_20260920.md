# KitchenSocketLeft stock-to-PM-Client OTA ABORT: evidence and next gate

Date: 2026-09-20 (Europe/Prague). Target: `KitchenSocketLeft`, IEEE `0xa4c138241e3de538`, stock `_TZ3000_b28wrpvx` / `TS011F` Router. **No second OTA is approved by this document.**

## Observed, not inferred

- Target-only `cli6` stock wrapper SHA-256: `b8c1d7a797a33936ac26f26305633b58369b0f409cda7d31e6291bc76851debe`; size 159858 bytes. Zigbee2MQTT read-only index check returned `update_available=true` and the exact image URL. This checks server eligibility, not acceptance by the stock firmware.
- Update transaction `bseed-ota-5d8f6e4b497543d985d45711fb3c2d53`, 2026-09-20 13:44:47–13:45:13 local time: initial progress 0%, then Zigbee2MQTT reported `Upgrade End` status `ABORT`. Response had `data:{}`. The original monitor missed this matching-transaction error; fixed in `helper_scripts/bseed_targeted_z2m_ota.py` and tested in `tests/test_bseed_targeted_z2m_ota.py` (commit `c43bb3bc`).
- The subsequent live device read returned relay ON, 0 W, stock Router identity, 236 V and LQI 168–172. The unsuccessful attempt did **not** establish a Client conversion. The interrupted local `ACTIVE_LOCK.json` still needs explicit reconciliation.
- Offline binary verification: the stock wrapper and native `cli6` are byte-identical from Zigbee OTA byte 56 onward; Telink subelement length, embedded image-size field, firmware magic and stored CRC-32 all check out. Published stock-to-PM-Router and experimental `cli4` stock wrappers also pass these structural/CRC checks. **Do not treat these checks as proof of stock-bootloader acceptance.**

## Plausible causes requiring discrimination

- Firmware acceptance rule: the stock OTA client may reject the new Client payload/type or first image data. A stock-facing outer OTA header and valid CRC do not establish acceptance by this stock firmware revision. We have no authenticated record of the exact historical HifiLeft direct-conversion transfer image.
- OTA data path/radio: configured `default_maximum_data_size=100` bytes and `image_block_response_delay=250` ms yielded a 1599-chunk/4-per-second initial estimate. Zigbee2MQTT documents a 50-byte default; actual blocks depend on the device request. The displayed initial 0% does **not** prove zero data blocks arrived. A smaller block size is a diagnostic variant, not a confirmed fix.
- Less supported at present: corrupt served image, wrong Zigbee target, or malformed OTA/Telink header; the hashes, identities, structural checks, and check response contradict these simple failure explanations.

## Required diagnostic gate before any repeat or two-step migration

1. Preserve the existing target's stock state and lock/log evidence; inspect the relay/load and any parallel OTA. Do not use the failed lock as permission to launch another OTA.
2. Capture a **target-scoped** Zigbee2MQTT debug trace of the next OTA exchange (`zhc:ota` and relevant device/ZCL messages) to establish the actual `queryNextImageRequest` parameters, offered image, `imageBlockRequest` offsets/max size, sent `imageBlockResponse` blocks and `upgradeEndRequest` status. Record first/last offsets; distinguish rejection before blocks from a stalled/repeated block exchange. The earlier info-level log does not contain this packet sequence.
3. Only after checking the trace, choose and document a single controlled variable: a safer 50-byte response limit for a radio-path hypothesis, a genuinely compatible image for an image-validation hypothesis, or the separately accepted published stock-to-Router route followed by a *separately evaluated* Router-to-Client update. Do not automatically retry the identical `cli6` wrapper.
4. Independently verify post-update role, software build ID, endpoint-2 relay ON, reporting, parent/rejoin and metrology; 100% OTA progress alone is insufficient.

## Subsequent read-only investigation (2026-09-20)

- Zigbee2MQTT rotated the prior failed-update records into `/config/zigbee2mqtt/log/2026-09-20.06-16-10/log1.log`; the active `log.log` alone no longer contains the event. A private local extract of the original 13:44:42–13:45:19 interval was saved on Zephyrus as `C:\Workspace\scratch\bseed-client-live-canary\kitchen_left_failed_ota_archived_window_20260920.txt`. The archive shows the update start at 13:44:47 and `ABORT` at 13:45:13, but contains **no individual image-block requests or responses** at its original info logging level. Do not claim zero firmware blocks transferred solely from displayed 0%.
- A `ROUTE_ERROR_MANY_TO_ONE_ROUTE_FAILURE` for network address `14934` appears at 13:45:05. KitchenSocketLeft's Zigbee2MQTT network address was `23212`; this single error is **not identified as a route failure for the target**.
- Verified bridge runtime `ota.default_maximum_data_size=100`, `image_block_response_delay=250`, `image_block_request_timeout=150000`. Zigbee2MQTT documents 50 bytes as the default and notes some devices reject >50/64. A 50-byte *per-request* limit is a controlled future transport diagnostic, **not a proven fix**. Do not lower the global fleet OTA settings or retry automatically.
- OTA-specific debug levels were temporarily accepted at runtime for a read-only update check, which again returned `update_available=true`. Sending an empty namespace map did not remove existing entries because runtime options merge objects; both OTA namespaces were explicitly reset to the effective original `info` level, which was verified through `bridge/info`. The map now contains two explicit `info` entries; a clean removal can be evaluated separately without restarting Zigbee2MQTT.
- Current HA/Z2M database still identifies KitchenSocketLeft by the same IEEE as the successful-interview stock Router, relay ON. No additional image transfer was started in this iteration.

### Historical control: HifiLeft is not an equivalent stock OTA client

A read-only search of archived Zigbee2MQTT logs found HifiLeft custom-firmware OTA attempts on 2026-09-19 with the same **1599-chunk / 399.75-second initial estimate** under the 100-byte maximum setting. Those attempts progressed to 0.81%, 7.51%, or 3.3%, respectively, before terminating in **150-second or 600-second communication timeouts**, not `ABORT`. KitchenSocketLeft's stock firmware instead sent `ABORT` 26 seconds after its OTA began, before the first periodic progress report. This establishes **different observed failure modes**, not that KitchenLeft received no blocks or that its cause is necessarily image validation. Because HifiLeft's recorded attempts involved an already-custom Client, they do **not** validate 100-byte blocks or the exact stock-to-Client package on pristine stock KitchenLeft.

## Controlled 50-byte follow-up: attempted 2026-09-20 14:36–14:41 Europe/Prague

**Outcome: `ABORT` again, no Client installation.** A new private, target-only campaign used the *same* locally hash-verified stock-facing `cli6` image (`b8c1d7a797a33936ac26f26305633b58369b0f409cda7d31e6291bc76851debe`) and specified `default_maximum_data_size=50` **in the MQTT update request**, without changing Zigbee2MQTT's global OTA settings or restarting the coordinator. Device IEEE: `0xa4c138241e3de538`; transaction: `bseed-ota-4d2bc9015a49471fa4fdb00196b13453`. A separate private lock records `phase=update_error`, the terminal transaction-matched Zigbee2MQTT error and no automatic retry permission.

- Read-only eligibility check passed; initial OTA estimate changed from 1599 chunks at the previous 100-byte setting to 3198 at 50 bytes. The actual **device-requested** maximum was **64 bytes**; the actual prepared block payloads were **50 bytes**. Do not infer that the previous 100-byte setting sent 100-byte blocks: Zigbee2MQTT caps each block to the requested maximum, and the first attempt lacks a block-level trace.
- The new Zigbee2MQTT namespaced debug trace shows **823 successive unique offsets** (`0, 50, 100, …, 41100`) and **nine requests for the same offset `41150`**. Every recorded `Payload offsets` entry had `dataSize=50`; the final nine each prepared bytes `[41150, 41200)`. The repeated requests occurred at 14:40:41, :43, :45, :47, :49, :53, :55, :57, :59. Zigbee2MQTT then reported device `ABORT` at 14:41:00; last published periodic progress was **25.74%**. Repeated preparation does **not** independently prove RF delivery or successful flash write. No other repeated offset occurred in the captured trace.
- The 50-byte block at `[41150, 41200)` contains ordinary non-padding firmware bytes and its neighboring blocks were not obviously empty; no local OTA-header, length or Telink CRC inconsistency was found. Its firmware-image-relative location is `41150 - 62 = 41088` (`0xA080`); the significance, if any, of this address to the **stock** OTA implementation has not been established.
- The live read-only GET after failure found **original stock Tuya Router** `_TZ3000_b28wrpvx`/`TS011F`, relay **ON**, reported **0 W**, voltage **236 V**, link quality **128**. No Client role/build was observed. No relay switching, joining window, global OTA configuration change, broker/Zigbee2MQTT/coordinator restart or third flash was performed. The temporary `zh:controller:ota` and `zhc:ota` debug levels were restored to their original `info` values.

**Revised conclusion:** Smaller responses allowed this diagnostic to progress well beyond the earlier early abort, but did not resolve the conversion. The exact repeated-offset signature suggests a localized block reception/acceptance or write stall. It does **not** distinguish RF/ZCL delivery from stock-firmware flash/validation behavior; do not claim the exact byte content or an `0xA000` flash boundary caused the abort. The first attempt's actual block sizes were not logged, so its different failure point cannot prove that excessive block size was its sole cause. Do **not** automatically retry this direct wrapper at 40, 50 or 64 bytes or count it as hardware-accepted.

**Next evidence gate:** determine the device-side cause of the nine repeated offset requests (stock OTA implementation/dump, correlated over-the-air packets and APS confirmations, or a comparable known-working stock→custom Router transfer) before selecting a different firmware route. A published stock→PM Router package is a **separate**, subsequently validated migration route; it does not automatically validate Router→Client or justify touching other outlets. Keep private runtime logs, backups and firmware candidates out of git.

## 2026-09-20 14:57+02:00: timeout and concurrency gate

- 50-byte run used `image_block_request_timeout=600000` (10 minutes); request `fileOffset=41150` arrived nine times between 14:40:41 and 14:40:59, followed by the device's `Upgrade End Request / ABORT` at 14:41:01. This is **not exhaustion of the coordinator's ten-minute wait**: the socket sent new requests approximately every two seconds and then independently ended its download. An implementation-specific stock-client retry limit or failure handling may have been reached; not verified without a client trace.
- Zigbee2MQTT debug printed `Payload offsets` indicating the block was constructed, not a positive device-side acknowledgement or successful flash write. No distinct radio/APS error was logged in that interval at the active info/OTA-debug levels. `41150 - 62 = 41088 = 0xA080` bytes into the embedded Telink image, i.e. 128 bytes beyond the `0xA000` boundary rather than an exact flash-sector crossing; no obvious magic/header transition was found in the block.
- At 14:59, an independent **TS0726-3-BS dimmer** (IEEE `0xa4c13843a9d40f85`) was actively receiving an OTA and had passed 79%; **do not launch KitchenSocketLeft OTA concurrently**. An isolated, private 32-byte-per-response diagnostic profile was prepared in `C:\Workspace\scratch\bseed-client-live-canary\kitchen_left_32b_diagnostic_20260920.py` with a fresh campaign directory. This is preparation only: no third KitchenSocketLeft flash has been submitted. The global OTA size and other-device update were not changed.
- Keep the `image_block_request_timeout=600000` value for a future controlled run; increasing the server wait alone would not change the observed client-side 20-second repeat/ABORT sequence. A 32-byte single-variable test could distinguish this block-boundary pattern from an incidental path failure, but no repeat is automatically authorized by the existence of a prepared script; verify no active OTA and the exact target preflight first.

## 2026-09-20 15:07–15:08+02:00: preflash OTA check failed independently

- Once the independent TS0726 dimmer OTA had completed and the mesh had settled, KitchenSocketLeft passed its private 32-byte campaign preflight (same stock IEEE/router, relay ON, 0 W, served-image hash checked). The subsequent read-only OTA check **did not receive a device response**. Zigbee2MQTT logged `Device didn't respond to OTA request` at 15:08:05, 60 seconds after sending `imageNotify`. **No third KitchenSocketLeft update was sent; no Client conversion was established.**
- The reusable targeted runner previously used a hardcoded 55-second check monitor deadline, which preempted Zigbee2MQTT's actual 60-second device-nonresponse error. This **real monitor bug** is corrected with a configurable `--check-timeout-seconds`, default 90 seconds and fail-closed minimum 70 seconds, with an offline regression test. This does not increase the stock socket's own retry limit and does not resolve its missing OTA query reply.
- A distinct private 32-byte profile exists but its update remains blocked until an independent OTA eligibility check succeeds again, no other OTA is active, and the target continues to pass live safety checks. Its overall **monitor-only** limit has been raised to 7200 seconds to avoid accidentally abandoning a slow but progressing small-block transfer; the per-block-request wait remains 600000 ms. Global Zigbee2MQTT OTA defaults remain unchanged.

## 2026-09-20 15:11–15:42 Prague: 32-byte transfer completed, but firmware boot/rejoin unconfirmed

- The third target-only `cli6` stock-facing OTA used the same 159858-byte image and verified SHA-256, `default_maximum_data_size=32`, image-block request wait 600000 ms, and a separate private workdir. The initial progress was 25.74%, matching the prior retained partial image. The block trace showed subsequent 32-byte requests beyond the previous repeated 41150 offset, then progress increased to 100%.
- Zigbee2MQTT logged `Finished update of 'KitchenSocketLeft'` at 15:32:47 and emitted an exact-transaction `status:ok` OTA response; wrapper version reported `192 -> 4294967295`. This confirms OTA transport completion **only**, not successful execution of `cli6`.
- At 15:32:52 Zigbee2MQTT attempted re-interview; at 15:34:57 it failed because it could not retrieve active endpoints. A read-only OnOff state query timed out at 15:36:08. The database and MQTT inventory still contain the **cached stock Router identity**, original bindings, `interview_completed:false` and no custom `software_build_id`; these records are not proof the original firmware is running.
- The read-only postflash verifier at 15:42:12 returned `unconfirmed`: Zigbee2MQTT bridge online, stale Router inventory, no completed interview, no verified custom EndDevice build, no new live target-state event. No relay toggle, power-cycle, reset, re-pair, firmware retry or global Zigbee restart was performed.
- The private 32-byte campaign's historical `update_ok` lock was archived verbatim and reconciled to `ota_transfer_ok_postflash_unverified`, with the failed postflash acceptance evidence. Future code also uses this phase directly and blocks another flash until separately verified acceptance. Keep old backups and original OTA traces private.

**Current diagnosis:** The earlier OTA block-size/transport failure is overcome, but the postflash boot/rejoin or firmware compatibility question is now the hard blocker. The actual live relay and attached-appliance state must not be inferred from old Zigbee2MQTT MQTT/database values. Determine whether the same IEEE announces and identify safe recovery before any physical intervention. A successful OTA transfer must not be described as a successful stock-to-Client conversion.
